# GameServer 压测报告

## 目的

测出当前 GameServer 在状态同步 tick + 快照下能扛多少并发房间,以及**瓶颈落在哪个组件**。

## 方法

新增压测工具 `tools/loadtest.cc`,**直连 GameServer:50051 模拟网关链路**。由于 `GateToGameEnvelope` 携带 uid,少数几条 TCP 连接就能驱动成千个虚拟单人房间:每个虚拟玩家 `CreateRoom → Ready`(触发 tick 启动 + 生成单位)→ 持续发 `MoveCmd` 让单位移动,服务器因此持续产出快照/增量。

核心指标:**有效 tick 频率**——从增量包里的 `server_tick` 随墙钟推进算出。健康时 ≈ 20Hz,服务器跟不上时下降。

构建:`linux-release`。机器:**4 核 / 16GB**(压测器与服务器**共用这 4 核**,见下方注意)。

```bash
loadtest <host> <port> <connections> <rooms_per_conn> <duration> <move_interval_ms>
```

## 结果

每房间 1 玩家、2 个持续移动的单位,每 100ms 一次 MoveCmd:

| 房间数 | 命令/s (offered) | 服务器消息/s | 有效 tick | GameServer CPU |
|---|---|---|---|---|
| 500   | 5k  | 10k | **20.0 Hz** ✅ | 0.28 核 |
| 1000  | 10k | 20k | **20.0 Hz** ✅ | 0.47 核 |
| 2000  | 20k | 40k | **20.0 Hz** ✅ | 0.97 核 |
| 4000  | 39k | 53k | **13.5 Hz** ⚠️ | 1.42 核 |

到 4000 房间时 tick 掉到 13.5Hz,**但服务器只用了 1.42/4 核**——说明不是总 CPU 不够,而是**某个单线程先满了**。

### 决定性证据:逐线程 CPU(4000 房间)

GameServer 共 9 个线程(1 网络 + 8 逻辑),取一个稳定窗口逐线程采样:

```
0.99 核  <- 网络线程 (NetworkShard io_context)   ← 打满
0.05 核  x8  <- 8 个逻辑分片线程                   ← 几乎全闲
```

**单一网络线程被钉在 100%,8 个逻辑线程几乎不出汗。** 整个 GameServer 被这一个网络线程卡住:所有入站解包 + 所有出站(路由、建 `GameToGateEnvelope`、序列化)都挤在它上面。

## 结论

1. **逻辑层分片是有效的**:8 个逻辑线程负载均衡且远未饱和,room_id 分片把房间均匀散开了。
2. **瓶颈是单网络线程**,不是聚合 CPU。即使给更多核,这一个线程也会先到 1.0 核封顶,而逻辑线程空转。
3. **当前容量**:在这台(且与压测器争抢的)4 核机器上,**约 2000 个并发 ticking 房间**稳定 20Hz;4000 房间开始落后。对本项目的目标规模(几个房间)绰绰有余。

## 优化:网络层分片(before / after)

根据上面的定位,把网络层也做成**分片的无锁 actor**:N 个 `NetworkShard`,每个自带 io_context + 线程 + acceptor(`SO_REUSEPORT` 绑同一端口,内核负载均衡连接)+ 自己那批链路 slots。关键设计:

- 每个网络 shard 单线程 → **依旧无锁,无需 mutex/strand**(没有给单个 shard 加锁)。
- `uid → 路由坐标{net_shard, link, generation}` **下放到逻辑 shard**(本来就单线程);回包时逻辑 shard 按坐标分组,投给对应网络 shard,网络 shard 用 link_id 在自己的 slots 里找 session。整条回包路径无锁。

压测器增测:每秒消息量、Ping/Pong 往返延迟(avg / p95)、服务器 CPU、RSS。
往返探测和快照出站走同一个网络线程,因此能反映负载下的响应性。

全指标对比(`linux-release`,4 核,4 连接,8s):

| 配置 / 房间 | recv msgs/s | avg 延迟 | p95 延迟 | tick | CPU | RSS |
|---|---|---|---|---|---|---|
| **1 shard / 4000** | 41k | **2449 ms** | **5304 ms** | 10.3 Hz | 1.49 核 | 65 MiB |
| **4 shard / 4000** | 81k | **10.6 ms** | **21 ms** | 20.0 Hz | 1.19 核 | 15 MiB |
| 4 shard / 2000 | 41k | 5.7 ms | 12 ms | 20.0 Hz | 0.67 核 | 12 MiB |
| 4 shard / 6000 | 85k | 1154 ms | 4092 ms | 14.3 Hz | 1.82 核 | 63 MiB |

同样 4000 房间,网络分片的效果:
- **p95 延迟 5304 ms → 21 ms(~250×)**,avg 2449 ms → 10.6 ms;
- 出站吞吐 41k → **81k msgs/s**(单 shard 时一半的快照根本发不出去);
- tick 10.3 → 20 Hz;CPU **反而更低**(1.49 → 1.19 核)——不再被单线程串行卡住;
- **RSS 65 → 15 MiB**:单线程满载时发送队列(`send_que_`)积压,内存被未发出的包撑大;分片后队列不积压,内存回落。这条很说明问题——延迟、内存、吞吐一起恶化,根因都是同一个饱和的线程。

4 shard 在本机(4 核、且与压测器争 CPU、仅 4 连接致 SO_REUSEPORT 分布不均)的拐点在 4000~6000 之间;6000 时队列又开始积压(延迟/RSS 回升)。真实多核 + 多连接环境下上限更高。

### 压测环境的局限(诚实标注)
- 4 核机器上**压测器与服务器争抢 CPU**,绝对上限偏保守。
- loadtest 只用了 **4 条连接**,SO_REUSEPORT 把 4 条连接散到 4 个 shard 不够均匀(8000+ 房间时仍可能一个 shard 被打满)。真实 Gate 有 64 条连接,分布会均匀得多。
- 要刻画"分片后的真实新上限",需要**核更多的机器 + 更多连接**(压测器最好独立部署)。

### 结论
"测量 → 定位单网络线程瓶颈 → 网络层分片(保持无锁)→ 重跑同一基准 → tick 11/9Hz 回到 20Hz、容量 2–3×"——这是一条完整的优化闭环,且改动严格遵循既有的分片 actor 架构,没有引入锁。

## 极限压测(尽力而为)

为逼近真实上限,改用 **8 连接**(让 SO_REUSEPORT 把链路均匀分到 4 个网络 shard)+ **低指令频率**(move_interval=1000ms,单位持续移动,所以增量出站满载、但命令/入站负载很小,以隔离出真正的出站瓶颈),逐步加房间:

| 房间数 | tick | avg 延迟 | p95 延迟 | CPU | RSS |
|---|---|---|---|---|---|
| 6000  | 20.0 Hz | 27 ms | 66 ms | 0.97 核 | 19 MiB |
| 8000  | 20.0 Hz | 14 ms | 21 ms | 0.83 核 | 22 MiB |
| 10000 | 20.0 Hz | 108 ms | 252 ms | 1.43 核 | 27 MiB |
| 12000 | 20.0 Hz | 120 ms | 256 ms | 1.57 核 | 29 MiB |
| 16000 | 14.7 Hz | 874 ms | 3808 ms | 2.11 核 | 169 MiB |
| 24000 | 14.4 Hz | 1100 ms | 3976 ms | 2.31 核 | 275 MiB |
| 32000 | 11.0 Hz(仅 15k 入局) | — | — | 2.47 核 | 672 MiB |

观察:
- **tick 吞吐**:健康 20Hz 一直撑到 ~12000 个轻量房间,只用 ~1.5/4 核;~14000+ 才开始掉到 ~14Hz。
- **延迟拐点早于 tick 拐点**:p95 在 ~8000 房间内保持 ≤70ms,之后因"每个 tick 8 个逻辑 shard 同时把出站灌给网络 shard"的**突发**而上升(均值 CPU 不高,但每个 tick 窗口内瞬时打满)。
- **分片确实消除了单线程瓶颈**:24000 房间时逐线程采样显示 **4 个网络线程各 ~0.37–0.41 核、均衡、没有一个被钉死**——和分片前"一个线程 100%、其余空闲"正好相反。
- **掉档时服务器并未自身饱和**:CPU 仅 ~2.3/4 核、无单线程打满 → 真正的限制是**这台 4 核机器被"服务器 + 同机压测器"一起占满**,不是服务器架构。

## 玩法负载压测(经济: 采集 + 训练)

加入资源经济/建造/训练后,扩展 loadtest 真正驱动玩法:进房发一次 HarvestCmd(工人进入
持续采集→返还自治循环)+ 周期性 TrainUnitCmd(训练队列一直运转),不再只是移动。
8 连接、network_shards=4:

| 房间数 | tick | avg 延迟 | p95 延迟 | CPU | RSS |
|---|---|---|---|---|---|
| 1000 | 20.0 Hz | 0.7 ms | 3.8 ms | 0.26 核 | 12 MiB |
| 2000 | 20.0 Hz | 0.8 ms | 5.1 ms | 0.41 核 | 16 MiB |
| 4000 | 20.0 Hz | 2.1 ms | 14 ms | 0.89 核 | 23 MiB |
| 8000 | 14.0 Hz | 1016 ms | 4109 ms | 2.09 核 | 136 MiB |

- 健康: ~4000 个"经济房"稳 20Hz(<1 核, p95 14ms);8000 崩(队列积压, RSS 飙升)。
- 注意: 每房只有 ~2 个工人, 经济状态机/训练队列每 tick 开销很小, **主导成本仍是增量出站
  (网络)+ 4 核机被服务器与压测器一起占满**(8000 崩时服务器才 2.09/4 核)。
- **这不是"完整重房"的真实成本**: 真实 RTS 房是 8 人 × 几十单位, 单房重几十倍, 真实并发房
  会降到几百量级(会话制游戏的常态: 少量重对局/机 + 横向扩)。要测真实单房成本需每房 spawn
  几十个单位 + 加入战斗。

## 重房压测(每房 50 单位持续移动)

为逼近真实对局的"单房很重"成本, 临时把每房初始单位调到 50、压测器驱动全体单位持续移动
(远目标, 单位整局到不了 -> 每 tick 全部 dirty)。8 连接、network_shards=4:

| 总单位数 | 房间×单位 | tick | avg | p95 | CPU | RSS |
|---|---|---|---|---|---|---|
| 10,000 | 200×50 | 20.0 Hz | 0.26 ms | 0.9 ms | 0.13 核 | 11 MiB |
| 40,000 | 800×50 | 20.0 Hz | 0.76 ms | 2.8 ms | 0.43 核 | 19 MiB |
| 80,000 | 1600×50 | 20.0 Hz | 0.83 ms | 3.8 ms | 0.89 核 | 30 MiB |
| 160,000 | 3200×50 | 20.0 Hz | 12 ms | 73 ms | 1.75 核 | 52 MiB |
| 320,000 | 6400×50 | 9.6 Hz | 394 ms | 2.2 s | 3.05 核 | 1.1 GB |

关键结论:
- 服务器能在 20Hz 下稳定推进 **~16 万个同时移动的单位**(~3200 房×50, p95 73ms, 1.75/4 核);
  32 万崩(CPU 近饱和 + 发送队列积压, RSS 1.1GB)。
- **直线移动 + protobuf 序列化本身极便宜**, 所以网络/序列化/tick 这套基础设施不是单位数瓶颈
  (分片已解决出站)。
- **但这是"直线移动, 无寻路 / 无碰撞 / 无真实战斗 AI"**。真实 RTS 的贵处是 A* 寻路(每单位贵
  10-100×)、碰撞/转向(O(邻居))、目标选择。加上这些后单位成本飙升, 容量会从"16 万简单单位"
  掉到"几千 AI 单位"量级, 即几百个完整对局房(会话制游戏常态)。
- 一句话: **当前瓶颈是网络出站(已用分片解决), 下一个瓶颈是仿真 AI/寻路(尚未实现), 而非框架。**
- (复现: 临时 kStartUnitsPerPlayer=50 + loadtest 驱动全体单位远目标移动; 已还原, 不入主干。)

## 方案A 路径压测(客户端寻路 + 服务器折线跟随)

方案A: MoveCmd 带客户端算好的 NavMesh waypoints, 服务器沿折线推进(寻路在客户端, 服务器
不跑 A*)。临时 50 单位/房, 进房发一条 30-waypoint 路径, 服务器整局沿折线走:

| 总单位 | tick | avg | p95 | CPU | RSS |
|---|---|---|---|---|---|
| 80,000  | 19.1 Hz* | 0.8 ms | — | 0.74 核 | 106 MiB |
| 160,000 | 20.0 Hz | 5.4 ms | 11 ms | 1.41 核 | 111 MiB |
| 320,000 | 18.6 Hz | 25 ms | 94 ms | 2.59 核 | 212 MiB |

(* 80k 那档 ramp-up 没满, 1557/1600 入局; 干净对照看 160k。)

关键结论:
- **方案A 几乎不改变服务器性能画像**: 160k 单位 @20Hz / 1.41 核, 和直线版同量级(直线 1.75 核)。
  折线跟随的服务器成本可忽略。
- 原因: **寻路在客户端, 服务器只做便宜的折线推进, 不跑 A***。所以"加寻路会把容量打下来"
  在方案A **不成立**——昂贵的寻路被甩给了客户端。
- 取舍: 服务器信任客户端路径(只校验速度/端点), 反作弊弱于服务器权威寻路。**只有方案B
  (服务器自己跑 A*/流场)才会把单房成本拉高、容量降到几百房量级。**
- (复现: 临时 kStartUnitsPerPlayer=50 + loadtest 发 30-waypoint MoveCmd; 已还原, 不入主干。)

### 这台机器测不出真上限(诚实结论)
压测器和服务器**同机争抢 4 核**:掉档时服务器只用了一半核、网络线程也没打满,说明瓶颈在"机器 + 压测器",不在服务端。**要测真实上限,必须把压测器放到独立机器、用核更多的服务器、并加重每房间的 sim(战斗/寻路/更多单位)以贴近真实对局。** 本机能给出的可信结论是:

- 健康区间(20Hz、p95 ≤ ~70ms):**~8000 个轻量房间**;
- 分片把负载均匀摊到各网络线程、不再有单线程瓶颈;
- 残余限制来自共享的 4 核 + 同机压测器,而非服务器本身。

## 注意

4 核机器上压测器与服务器争抢 CPU,绝对吞吐数字偏保守。但**逐线程结论不受影响**:服务器自身负载严重失衡(1 线程满、8 线程闲)证明瓶颈是单线程串行,与外部争抢无关。

## 背压: 有界发送队列(可靠性)

压测中发现一个真实可靠性问题: **过载时发送队列(send_que_)无界增长, 内存爆炸**。
修复: 给每个连接的发送队列加上限 `kMaxSendQueueDepth=2048`, 满则丢弃最旧的包
(状态同步靠下一帧全量快照重新对齐), 优雅降级而非 OOM。三处会话(GatewayLinkSession /
Csession / ClientSession)统一加上。

同一过载场景对照(6400 房 × 50 单位 = 32 万单位持续移动):

| | 无界队列(之前) | 有界 + 丢最旧(现在) |
|---|---|---|
| RSS | ~1100 MiB | **108 MiB**(~10×↓) |
| tick | 9.6 Hz(崩) | **19.8 Hz**(稳住) |

不仅内存被压住, **tick 反而稳了**——因为服务器不再被巨大积压拖死, 而是丢掉过时增量、
保持响应。这是教科书级的**背压 / 优雅降级**: 过载时主动丢可恢复的数据, 保住延迟与稳定性。
丢包数通过 send_dropped_ 计数(后续接入 metrics)。

## 全玩法压测(采集+建造+训练+战斗 同时跑)

为得到最贴近真实对局的房间容量, 临时每房 16 单位, 压测器**每房同时驱动全部 5 个系统**:
大部分工人采集(经济)、1 工人建资源站(建造)、周期产兵(训练)、单位持续攻击(战斗,
触发追击/射程/冷却/伤害/死亡)。8 连接、network_shards=4:

| 房间 | 单位 | tick | 客户端 p95 | 服务器 tick p99 | drops | CPU | RSS |
|---|---|---|---|---|---|---|---|
| 1000 | 16k | 20Hz | 4.7 ms | 4.5 ms | 0 | 0.25 核 | 17 MiB |
| 2000 | 32k | 20Hz | 6.7 ms | 7.8 ms | 0 | 0.42 核 | 25 MiB |
| 4000 | 64k | 20Hz | 17 ms | 15 ms | 0 | 0.80 核 | 40 MiB |
| 8000 | 128k | 20Hz | 533 ms | 26 ms | 179627 | 1.83 核 | 75 MiB |

- **全玩法健康容量: ~4000 房(64k 单位)@ 20Hz, p95 17ms, <1 核, 零丢包。** 拐点在 4000–8000 之间。
- 8000 房: 背压开始丢包(drops 18 万)、客户端延迟飙到 533ms, **但 tick 仍 20Hz**——背压在优雅
  降级(丢过时增量保 tick, 而非 OOM/崩)。背压触发被 /metrics 的 send_drops 实时捕获(0 → 18 万)。
- 注意: 1 人房 + 友军误伤来触发战斗代码(非 2 队真实对战); 移动是直线/折线(方案A 客户端寻路);
  4 核机与压测器争 CPU; 16 单位/房属"中等"。真实 8 人重房单位更多, 房数会更低。
- (复现: 临时 kStartUnitsPerPlayer=16 + loadtest 全玩法驱动; 已还原, 不入主干。)

## 真 2 人对战压测(16v16: 采集 + 战斗 + 训练)

压测器升级为 2 玩家房: A 建房 → B 加入 → 两人 ready 开局(两队各 16 单位), 之后双方各自
采集 + **攻击对方单位**(真实 2 队对战, 触发追击/伤害/死亡)+ 周期产兵。8 连接、network_shards=4:

| 2P 房 | 玩家 | 单位 | tick | 客户端 p95 | 服务器 tick p99 | drops | CPU | RSS |
|---|---|---|---|---|---|---|---|---|
| 1000 | 2000 | 32k | 20Hz | 6.3 ms | 3.0 ms | 0 | 0.24 核 | 21 MiB |
| 2000 | 4000 | 64k | 20Hz | 6.6 ms | 7.4 ms | 0 | 0.41 核 | 33 MiB |
| 4000 | 8000 | 126k | 20Hz | 22.8 ms | 18 ms | 3789 | 0.81 核 | 55 MiB |

- **真 2 队对战健康容量: ~2000 房 / 4000 玩家 / 6.4 万单位 @ 20Hz, p95 6.6ms, 0.41 核, 零丢包。**
  4000 房开始轻微背压丢包(接近拐点)。
- 和单人全玩法测(~4000 房)同量级 → 战斗/经济的单房成本中等, 主导成本仍是单位数 × 出站。
- 注意: 仍无服务器寻路(方案A 客户端寻路)、4 核机与压测器争 CPU; 真实 8 人重房单位更多 → 房数更低。
- loadtest 的 "playing players" 计数器只认 envelope 的第一个 target_uid(同链路上 A/B 合包),
  会少计一半, 以服务器端 /metrics(rooms/units/tick)为准。
- (复现: 临时 kStartUnitsPerPlayer=16 + loadtest 2 玩家对战驱动; 已还原, 不入主干。)

## 端到端压测(网关 + 游戏服一起)

前面各档都是直连 GameServer 模拟网关链路。这里用**真实客户端**经 GateServer 全链路压:
每客户端一条到网关(:8888)的 TCP 连接, `LoginReq → CreateRoom → Ready → MoveCmd`;命令由
网关注入 uid + 打包转发到 GameServer, 回包由网关拆包后转发回客户端。工具: `tools/e2e_loadtest.cc`。

| 真实客户端 | login RTT avg/p95 | tick | GameServer CPU | GateServer CPU |
|---|---|---|---|---|
| 500  | 10 / 17 ms | 20Hz | 0.23 核 (14 MiB) | 0.27 核 (12 MiB) |
| 1000 | 46 / 87 ms | 20Hz | 0.48 核 (15 MiB) | 0.58 核 (16 MiB) |
| 2000 | 35 / 64 ms | 20Hz | 0.85 核 (19 MiB) | 1.00 核 (25 MiB) |

- **全链路打通且 20Hz 稳**(2000 真实客户端,每人独立连接)。
- **网关不是免费的**: Gate CPU 与 Game 同量级——每条消息过网关两次(客户端↔网关、网关↔游戏服)
  并拆/装 GateToGameEnvelope。**系统总开销 ≈ Game + Gate(约翻倍)**, 这是只测 Game 看不到的部分。
- login RTT 含"登录风暴"(数千客户端同时连入); 进入稳态后游戏 tick 稳定 20Hz。
- 注意: 真实场景每玩家一条到网关的连接; 4 核机现被 Game + Gate + 压测器三方分摊。
